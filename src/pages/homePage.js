import React from "react";
import { Header, Relation, Services } from "../components/index";

const HomePage = (props) => {
  return (
    <>
      <Header />
      <Services />
      <Relation />
    </>
  );
};

export default HomePage;
